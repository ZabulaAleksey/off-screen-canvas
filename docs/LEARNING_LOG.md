# Learning Log

- Проект остаётся минимальной браузерной демонстрацией без зависимостей и сборки.
- Основной риск — различия поддержки OffscreenCanvas; fallback является частью контракта.
- Worker flow проверяется вручную до появления автоматического test runner.
